%h1 Photo Details
%div.photo-gallery
  - @photo.followed_users.each do |user|
    %h2= user.name
    %div.slideshow-container
  - @photo.followed_users.each do |user|
    %h3= user.name
    - user.photos.order(created_at: :desc).each do |p|
      .mySlides
        = image_tag photo.image.url(:medium), class: "photo-image", data: { photo_id: photo.id, current_user: current_user == photo.user }
        %p.photo-title= p.title
%p
  %strong Title:
  = @photo.title
%h2 Thumbnail
= image_tag @photo.image.url(:thumb)